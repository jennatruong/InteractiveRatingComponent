import { StrictMode } from 'react';
import * as ReactDOM from 'react-dom';

import App from '../docs/src/components/App/App';

ReactDOM.render(
  <StrictMode>
    <App />
  </StrictMode>,
  document.getElementById('root')
);
